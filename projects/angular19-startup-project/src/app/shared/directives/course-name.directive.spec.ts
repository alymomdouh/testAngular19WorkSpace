import { CourseNameDirective } from './course-name.directive';

describe('CourseNameDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseNameDirective();
    expect(directive).toBeTruthy();
  });
});
